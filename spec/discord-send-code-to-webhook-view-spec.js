'use babel';

import DiscordSendCodeToWebhookView from '../lib/discord-send-code-to-webhook-view';

describe('DiscordSendCodeToWebhookView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
